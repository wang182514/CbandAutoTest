# 待办改进项

> 由 Agent 基于当前代码与产线使用场景整理。已完成的项会从此文件移除，历史改动见 `CHANGELOG.md`。

## 测试流程缺陷

### 仪器状态管理

- [ ] 测试开始前增加统一的仪器复位（`TestBase.reset_instruments()`）。
- [ ] `rx_nf.py` 异常时关闭 RX 电源。
- [ ] `tx_flatness_pn.py` 异常时关闭 VSG 射频并切回 CW 模式。
- [ ] 加载仪器模板前校验模板是否存在。

### 配置与边界处理

- [ ] 把 `tx_rx_influence.py` 中 TX 开启后的 10 秒等待改为可配置项。
- [ ] 把 `sa_marker_noise()` 中固定 3 秒等待改为可配置项。
- [ ] `tx_gain.py` 测试结束后将 SA offset 归零。
- [ ] 运行测试前校验序列号是否为空。
- [ ] 让 `default_settings.json` 中的测试 `enabled` 开关真正生效，并在 UI 增加对应复选框。

### 报告与输出

- [ ] `--headless` 模式运行结束后自动生成 TXT 报告。
- [ ] 修复 DOCX 模板路径默认指向项目根目录上级的问题。
- [ ] 截图失败时在 UI 结果中给出明确提示。

### 可靠性与可维护性

- [ ] 解决 `TestRunner.request_stop()` 无法中断阻塞 SCPI 查询的问题。
- [ ] 减少测试之间的状态耦合（如电源开关顺序）。

## UI 与交互

- [ ] 序列号输入增加正则校验。
- [ ] 设置对话框 9 个 Tab 过多，考虑合并为 5 个。
- [ ] 增加「打开报告目录」「打开截图目录」快捷入口。
- [ ] 日志区域对 PASS/FAIL 关键字高亮，并支持过滤错误。
- [ ] 窗口记住用户上次调整的尺寸。
